@using BTCPayServer.Plugins.13ggd.Plugins.MinimalCash
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<div class="row">
    <div class="col-md-12">
        <h3>Minimal Cash Payment (13ggd)</h3>
        <p>Click to mark as paid:</p>
        <div class="text-center">
            <button class="btn btn-primary" onclick="markAsPaid()">Mark as Paid</button>
        </div>
    </div>
</div>

<script>
function markAsPaid() {
    const invoiceId = '@Model.InvoiceId';
    const storeId = '@Model.StoreId';
    const returnUrl = `/i/${invoiceId}`;
    
    fetch(`/stores/${storeId}/minimalcash/MarkAsPaid?invoiceId=${invoiceId}&returnUrl=${returnUrl}`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'X-Requested-With': 'MinimalCashHttpRequester'
        }
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            alert('Payment marked as paid successfully!');
            window.location.href = returnUrl;
        } else {
            alert('Error: ' + data.error);
        }
    })
    .catch(error => {
        alert('Error: ' + error.message);
    });
}
</script>
